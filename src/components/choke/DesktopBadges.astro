---
// Secondary download links for the desktop builds. They sit below the Google
// Play badge because mobile is the primary target.
import { RELEASES_URL } from './config';
import { getChoke, type Lang } from '../../i18n/ui';
import { defaultLang } from '../../i18n/utils';

interface Props {
  variant?: 'dark' | 'ink';
  lang?: Lang;
}

const { variant = 'dark', lang = defaultLang } = Astro.props;
const t = getChoke(lang).desktop;
---

<div class:list={['desktop', variant]}>
  <span class="label">{t.label}</span>
  <div class="links">
    <a href={RELEASES_URL} target="_blank" rel="noopener" title={t.hint}>
      <svg width="18" height="18" viewBox="0 0 24 24" aria-hidden="true">
        <ellipse cx="8.6" cy="21.4" rx="2.6" ry="1.3" fill="#F5B800"></ellipse>
        <ellipse cx="15.4" cy="21.4" rx="2.6" ry="1.3" fill="#F5B800"></ellipse>
        <ellipse cx="12" cy="14.4" rx="6.3" ry="7.1" fill="currentColor"></ellipse>
        <circle cx="12" cy="6.4" r="4.3" fill="currentColor"></circle>
        <ellipse cx="12" cy="15.6" rx="4" ry="5.4" fill="#fff" opacity="0.92"></ellipse>
        <circle cx="10.4" cy="5.9" r="1.5" fill="#fff"></circle>
        <circle cx="13.6" cy="5.9" r="1.5" fill="#fff"></circle>
        <circle cx="10.5" cy="6.1" r="0.75" fill="#111"></circle>
        <circle cx="13.5" cy="6.1" r="0.75" fill="#111"></circle>
        <ellipse cx="12" cy="8.4" rx="1.9" ry="1.3" fill="#F5B800"></ellipse>
      </svg>
      {t.linux}
    </a>
    <a href={RELEASES_URL} target="_blank" rel="noopener" title={t.hint}>
      <svg width="16" height="16" viewBox="0 0 23 23" aria-hidden="true">
        <path d="M0 2.6 9.5 1.3V11H0z" fill="currentColor"></path>
        <path d="M10.7 1.15 23 0v10.9H10.7z" fill="currentColor"></path>
        <path d="M0 12.1h9.5v9.7L0 20.5z" fill="currentColor"></path>
        <path d="M10.7 12.1H23V23l-12.3-1.6z" fill="currentColor"></path>
      </svg>
      {t.windows}
    </a>
  </div>
</div>

<style>
  .desktop {
    display: flex;
    flex-direction: column;
    gap: 10px;
    margin-top: 16px;
  }

  .label {
    font-family: var(--font-mono);
    font-size: 11px;
    font-weight: 600;
    letter-spacing: 0.8px;
    text-transform: uppercase;
    color: var(--muted-2);
  }

  .links {
    display: flex;
    align-items: center;
    gap: 10px;
    flex-wrap: wrap;
  }

  .links a {
    display: inline-flex;
    align-items: center;
    gap: 9px;
    font-size: 14px;
    font-weight: 700;
    color: var(--text);
    border: 1px solid var(--border-strong);
    border-radius: 11px;
    padding: 9px 16px;
    transition:
      transform 0.15s ease,
      border-color 0.15s ease;
  }

  .links a:hover {
    transform: translateY(-1px);
    border-color: var(--green);
    color: var(--text);
  }

  /* On the green CTA banner the text and borders flip to white. */
  .ink .label {
    color: rgba(255, 255, 255, 0.8);
  }

  .ink .links a {
    color: #fff;
    border-color: rgba(255, 255, 255, 0.4);
    background: rgba(0, 0, 0, 0.18);
  }

  .ink .links a:hover {
    color: #fff;
    border-color: #fff;
  }
</style>
